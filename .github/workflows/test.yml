name: Test VM Execution

on: [push]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Run tests
        shell: powershell
        run: |
          cd C:\sil\gherkin\gherkin_silfahren_tests
          .\.venv\Scripts\Activate.ps1
          $env:IPG_ROOT="C:\tools\ipg\carmaker\win64-14.1.1"
          python run_behave.py --stage silver_carmaker --ini "config\golden.ini" --feature .\features\golden.feature --format=html-pretty
